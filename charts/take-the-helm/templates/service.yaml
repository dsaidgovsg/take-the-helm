apiVersion: v1
kind: Service
metadata:
  name: {{ include "take-the-helm.fullname" . }}
  labels:
    {{- include "take-the-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "take-the-helm.selectorLabels" . | nindent 4 }}
